export enum activeSection {
    bio,
    post,
    allTicket
}

export enum HoverSetSwiper {
    left,
    right,
    none
}

export enum statusData{
    found,
    notFound,
    load
}

export enum infoTicket{
    about,
    highlight,
    line_up,
    show_area,
    maps
}